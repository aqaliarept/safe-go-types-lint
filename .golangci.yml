linters-settings:
  custom:
    safe-go-types:
      path: bin/safe-go-types-lint
      description: Flags raw scalar types in struct fields and local variable declarations.
      original-url: github.com/your-org/safe-go-types-lint

linters:
  enable:
    - safe-go-types
